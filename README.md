# AiayGround Pages

GitHub Pages 전용 저장소입니다.

하는 일:

```
빠른 검증용 웹 프로토타입을 외부 URL로 배포한다.
로컬 서버, WSL NAT, Windows 방화벽, portproxy에 의존하지 않는다.
```

현재 슬롯:

```
prototypes/one-hand-necro-farm/
```

검증 질문:

```
수확할지 부패시킬지의 선택이 5분 동안 의미 있는 판단으로 느껴지는가?
```
